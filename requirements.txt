# Gradio 5 + HF Spaces
gradio>=5.0,<6
huggingface_hub>=0.25
spaces>=0.30

# Models — pin to versions known to work with our trained Qwen2.5/Phi-3/DeepSeek
transformers==4.46.3
tokenizers>=0.20,<0.22
accelerate>=0.30,<2
peft>=0.12,<0.15
bitsandbytes>=0.43
sentencepiece>=0.2

# PyTorch (HF Spaces ZeroGPU provides CUDA at runtime; pin loosely)
torch>=2.4

# Data
duckdb>=1.0
pandas>=2.1
numpy>=1.24,<2.0
openpyxl>=3.1

# Visualization
plotly>=5.20
kaleido>=0.2.1
